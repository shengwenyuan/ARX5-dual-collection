# 方舟无限 ARX5 双臂三路摄像桌面任务数据采集工具

设计一款专业、稳定、可持续扩展的桌面任务数据采集工具。每条 Episode 独立保存为 MCAP 与 JSON，是数据采集、验收和后续训练的最小单位。

## 场景

- 使用两只 ARX5-2025 主臂，每只主臂集成夹爪和示教器，由采集者直接操作。
- 系统不存在从臂和遥操作链路；采集程序只读取机械臂状态，不发送位置、回零、夹爪或 EEF 控制命令。
- 人工示教依赖 ARX5 官方 `remote_master` 重力补偿模式；允许 Vendor 节点在启动时进入 `G_COMPENSATION`，这是唯一冻结的控制例外。
- 左右手腕各安装一颗 RealSense D405；第三颗位于桌面正中上方约 50 cm，俯视桌面。
- 三颗摄像机通过 USB 接入采集主机。第三视角允许采集者手臂入镜。
- 录制开始与结束使用统一触发接口。当前没有脚踏板设备和驱动，v0.1 使用键盘信号替代，并预留脚踏板适配器。
- CAN 口及设备映射以真机上电结果为准，写入站点配置，不硬编码在业务代码中。

## v0.1 范围

实现：

- 官方驱动集成与设备启动。
- 固定 ROS 2 Topic、Episode 状态机和命令行入口。
- 键盘触发录制，预留脚踏板输入接口。
- MCAP 与 JSON 录制、临时写入、完整关闭和原子提交。
- 实际频率监督、录制结果检查和基础回放验证。
- 典型 90～150 秒 Episode，以及长时间连续开机采集。

暂不实现：

- UI 和人工 `fail` 标注。v0.1 正常结束记为 `success`，异常结束记为 `aborted`。
- 云端任务平台、自动上传和复杂任务分发。任务先由本地定义传入。
- DAgger 控制、失败回退和人工接管逻辑，只保留接口位置。
- 时间偏差分析、补偿和在线同步算法。
- 容器设备权限收敛与软件包发行。

## 运行与部署

- 容器系统固定为 Ubuntu 24.04，使用 ROS 2 Jazzy。
- 全部核心功能集成在一个 Docker Container 中，Docker Image 是 v0.1 的部署单位。
- v0.1 使用 `privileged container + host network`，优先保证 CAN、摄像机及后续 USB 输入设备可见和真机调试效率。
- 设备权限最小化属于后续部署优化，不作为首版验收条件。
- 本地 Mac 用于代码同步与提交；`w3-arx5` 用于容器部署、真机调试和最终验收。

## 驱动边界

依赖以下官方能力：

1. `ARXroboticsX/ARX_X5` 官方 SDK 及其 ROS 2 配置；明确排除 `ARX5_beta`。
2. RealSense D405 官方稳定 SDK。版本按现有固件的官方兼容矩阵选择，不追逐 beta 或不匹配的新版本。
3. 录制触发输入：v0.1 使用键盘，后续接入脚踏板及其他 USB 输入设备驱动。

驱动先以子仓或源码依赖集成，再封装成项目内部稳定接口。接口成熟后按实际语言形态整理为 ROS 2 Package、Python Package 或共享库，不要求所有驱动采用同一种打包方式。

## 录制契约

### 输入信号

- 三路摄像机：1280×720 彩色图像、与彩色图像对齐的 Depth，目标频率 30 Hz。
- 左右机械臂：六关节位置、六关节速度、`joint_currents` 原始值、夹爪状态、示教器输入状态、EEF 位姿，目标频率 60 Hz。
- ARX5 SDK 当前将反馈组合为 `[J1..J6, gripper]`；对外消息必须拆分六关节与夹爪，不把夹爪命名为第七关节。
- 不录制机械臂在线状态，不存在从臂信号。

### 时间语义

- 各 Topic 独立记录原始消息和时间戳。
- 禁止插值、补帧、重复帧和伪造同步帧。
- D405 不支持三机硬件触发同步。v0.1 独立记录三路数据，假设当前偏差可接受，不分析或修正偏差。
- RealSense 官方 SDK 可能提供多设备时间戳或软件同步能力。安装 SDK 后必须检索官方 API 与示例并真机验证；验证通过后再决定是否采用。
- `sn id` 的含义暂不明确，仅保留为开发观察项，发现真实需求后再展开。

### 频率监督

- 录制过程中显示各 Topic 的实时频率和累计帧数。
- JSON 保存实际帧数、平均频率和最大帧间隔。
- 频率偏低只产生警告，不自动判定 Episode 失败。
- 任一必需 Topic 完全停止时立即结束录制，并将 Episode 标记为 `aborted`。

### Topic

所有外部输入先映射为固定逻辑名称，例如：

```text
/sensors/camera_left/...
/sensors/camera_right/...
/sensors/camera_overview/...
/embodiments/left_arm/...
/embodiments/right_arm/...
```

摄像机属于外部感知，机械臂状态属于具身本体，因此使用不同根命名空间。Topic 不暴露 CAN 口、USB 口等物理编号。同一版本生成的 MCAP 必须具有一致的 Topic 名称和消息类型，不再做离线重命名。

## Episode

状态机保持简单：

```text
READY --录制触发--> RECORDING --录制触发--> FINALIZING --> COMPLETE --> READY
                         \--设备异常--> ABORTED -------> READY
```

录制时持续写入临时目录，不在内存中缓存整条 Episode：

```text
episodes/
  .<episode_id>.partial/
    episode.mcap
    metadata.json
```

正常关闭 MCAP 并写完 JSON 后，将整个目录原子重命名为正式 Episode。默认不生成 SHA；只有正式计划确认存在必要的完整性边界时才增加校验。进程意外退出时保留 `.partial` 目录，下一次启动只报告，不自动恢复。

`metadata.json` 至少包含：

- Schema 版本、Episode ID 和任务描述。
- `success`、`fail` 或 `aborted` 结果；`fail` 留给后续 UI 版本。
- 开始时间、结束时间和持续时长。
- 站点与采集设备编号。
- 预期频率、实际帧数、平均频率和最大帧间隔。
- 软件版本、容器版本及必要的设备配置。

Schema 必须可扩展，新增字段不得改变已有字段语义。

## 异常与代码风格

- 追求极致可读性，谨慎设计结构、抽象和封装。
- 主线只保留核心逻辑，不堆叠复杂防御代码。
- 设备接入、录制状态和文件提交属于必要边界，必须显式检查并快速失败，不能静默生成坏数据。
- 通过 `tests/` 覆盖正常路径、异常路径和数据契约，避免在业务主线内混入测试逻辑。
- 每个模块的语言选择和具体架构在对应正式计划中确定。
- 模块间通过稳定接口解耦，降低驱动替换和局部重构压力。

## 延伸功能

- UI：独立开发，提供任务选择、状态展示和人工 `fail` 标注。
- DAgger：预留模型执行、失败回退、人工介入 Topic 和切换时间点，不实现控制逻辑。
- 任务分发：v0.1 使用本地任务定义，后续再扩展为远程服务。
- 发行方式：Docker 稳定后再评估软件包或共享库交付。

## 开发与计划治理

- 本地只读资料使用 `rs-d400` 和 `arx5-docs` MCP。
- Google CI/CD Skills 只参考阶段划分、构建、测试、发布和回滚思想，不引入 GCP 运行依赖。
- 架构讨论必须主动指出风险、矛盾和替代方案，不只复述需求。
- 主动识别工具化机会：需要反复查询权威外部知识时考虑 MCP，稳定流程考虑 Skill，确定性本地操作考虑脚本或 CLI；有明确收益后再建议建设。
- 使用 `write-project-plans` Skill 管理计划：
  - 正式计划写入 `docs/<module>/`；架构和部署等内容可分别进入 `docs/architecture/`、`docs/deployment/`。
  - 轻量、临时计划写入 `plans/`。
  - 每轮开发或部署完成后覆盖更新 `plans/last_edit.md`，压缩记录上一轮修改。
  - 测试验收完成后，将结论简要回写到原正式计划。

## 首版验收重点

- 容器能够同时访问双臂和三颗 D405。
- 键盘信号可以连续控制多条 Episode 的开始与结束；脚踏板只验收接口预留。
- 三路 1280×720 RGB-D @ 30 Hz 能够连续录制至少 150 秒，并记录 USB、实际频率和磁盘吞吐表现。
- 90～150 秒录制可完整关闭，MCAP 可读取，JSON 与 MCAP 一一对应。
- 长时间运行期间无持续资源泄漏，实际频率和异常中断可被观察。
- 必需 Topic 停止时生成 `aborted` Episode，不生成表面成功的坏数据。

## 开发前置确认

- `w3-arx5` 已确认左臂为 `can1`、右臂为 `can3`；业务代码仍只读取站点配置。
- 双臂使用官方 `v2_collect` 的 `remote_master` 语义，启动时必须进入重力补偿；采集侧不得发布 `/arx_joy` 或任何运动控制 Topic。
- 已发现独立 `ARX_KEY` USB 设备；其与示教器输入的关系和字段语义需真机辨识后再冻结。
- 安装 RealSense 官方 SDK 后查找多设备同步相关 API、配置和示例，区分软件能力与硬件触发能力。
- 在 `w3-arx5` 上先完成三路 720p RGB-D 的 USB 与磁盘吞吐基准，再冻结编码和 MCAP 压缩策略。
